# ts-money fixture

Greenfield TypeScript fixture: implement an immutable `Money` value type from a
stub. Zero third-party dependencies — uses only Node's built-in test runner
(`node --test`) and `tsc` for type checking (run on demand via `npx`).

- Implement `src/money.ts` so all tests in `tests/money.test.ts` pass.
- Do not edit `tests/`.

{
  "name": "rate-limiter-buggy",
  "private": true,
  "version": "1.0.0",
  "description": "Express-style rate limiter middleware fixture (seeded off-by-one bug)",
  "main": "src/index.js",
  "scripts": {
    "lint": "npx eslint src/ test/ scripts/",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/test/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  }
}
